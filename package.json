{
	"name": "triggerable-generation",
	"version": "1.0.2",
	"description": "Trigger a generator, again and again. Output as async generator.",
	"main": "triggerable-generation.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "https://archive.voodoowarez.com/triggerable-generation"
	},
	"keywords": [],
	"author": "rektide de la faye",
	"license": "X11",
	"module": "triggerable-generation.js",
	"dependencies": {
		"defer-esm": "^1.0.0"
	}
}
